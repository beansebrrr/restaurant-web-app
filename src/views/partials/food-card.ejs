<% 
  const description = `${food.description.slice(0, 40)}${food.description.length > 40 ? "..." : ""}`;
  const priceStr = `AED ${food.price.toLocaleString ("en-AE", { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`
%>

<li role="link" class="food-card">
  <a href="/menu/<%= food.id %>"></a>
  <%- include('./food-thumbnail', { food: food, className: "food-card__thumbnail" }) %>
  <div class="food-card__contents">
    <h2 class="food-card__name"><%= food.name %></h2>
    <p class="food-card__description"><%= description %></p>
    <div>
      <p class="food-card__price"><%= priceStr %></p>
      <button class="food-card__button"
      hx-post="/basket"
      hx-vals='js:{ "itemID" : "<%= food.id %>" }'
      hx-swap="none">+</button>
    </div>
  </div>
</li>